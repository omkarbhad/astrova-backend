# Production Environment Variables
PORT=10000
EPHE_PATH=./ephe
DB_PATH=./kundali.db

# CORS Origins (comma-separated)
# Add your frontend domain here
CORS_ORIGINS=https://your-frontend-domain.onrender.com,https://localhost:3000

# Optional: Database URL for PostgreSQL (if switching from SQLite)
# DATABASE_URL=postgresql://username:password@host:port/database

# Optional: Secret key for any authentication
# SECRET_KEY=your-secret-key-here